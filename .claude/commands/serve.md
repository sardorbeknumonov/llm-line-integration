Start the development server using the Claude Preview tool.

Use `preview_start` with the "dev-server" configuration from `.claude/launch.json` to start the FastAPI server. Then take a screenshot to confirm it's running.

After starting, remind me to:
1. Run `ngrok http 8000` in a separate terminal
2. Set the webhook URL in LINE Developer Console
