Generate a text-based flow diagram of the current conversation state machine.

Read `app/handlers/conversation_handler.py` and output:
1. A visual state transition diagram (text-based)
2. A table of all states with: state name, entry trigger, message type sent, possible exits
3. Count of total states and transitions

This helps me understand the current conversation flow at a glance.
